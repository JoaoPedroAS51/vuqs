import { describe, expect, it } from 'vitest'

// Placeholder so the unit suite is not empty before the first module lands.
describe('vuqs', () => {
  it('placeholder', () => {
    expect(true).toBe(true)
  })
})
